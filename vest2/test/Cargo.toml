[package]
name = "test"
version = "0.1.0"
edition = "2021"

[dependencies]
vstd = "0.0.0-2026-03-17-2326"
vest_lib = { path = "../../vest" }

[package.metadata.verus]
verify = true

[package.metadata.verus.ide]
extra_args = "--expand-errors"
