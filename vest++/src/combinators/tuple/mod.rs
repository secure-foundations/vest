//! Sequential (tuple) composition for pairs.
//! N-ary formats are built by nesting: `(A, (B, C))`.
/// Correctness proofs for this combinator.
pub mod proof;
/// Specification trait implementations for this combinator.
pub mod spec;
